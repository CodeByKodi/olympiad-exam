/**
 * Admin Mode configuration.
 * Change this key to customize admin access.
 * Not intended as secure authentication - only for UI role separation.
 */
export const ADMIN_KEY = 'olympiad-admin-2026';
